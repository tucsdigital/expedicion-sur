"use client";

// Referencia viva del home aplicado en Expedicion Sur.
// Se mantiene este archivo en `ejemplos` para no romper el build
// y para dejar una entrada clara hacia la implementación real.

export { default } from "@/components/HomeClient";
